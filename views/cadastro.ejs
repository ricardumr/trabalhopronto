<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IFStore - Cadastro</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #5e2a84; /* Fundo roxo */
            font-family: 'Arial', sans-serif;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            animation: fadeIn 1s ease-in-out; /* Animação do fade-in do corpo */
        }

        .form-container {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            padding: 30px;
            width: 100%;
            max-width: 700px; /* Aumentei a largura máxima do formulário */
            text-align: left;
            opacity: 0; /* Inicialmente invisível */
            animation: formSlideIn 1s ease-out forwards; /* Animação do slide-in */
        }

        h1 {
            color: #333;
            font-size: 1.8em;
            margin-bottom: 20px;
            text-align: center;
        }

        .logo-container {
            text-align: center;
            margin-bottom: 20px;
        }

        .logo-container img {
            width: 150px; /* Tamanho da logo */
            height: auto;
        }

        label {
            font-size: 1em;
            margin-bottom: 8px;
            display: block;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .input-container {
            display: flex;
            justify-content: space-between;
            gap: 20px;
        }

        /* Estilo dos inputs */
        .form-control {
            width: 100%;
            padding: 18px;
            margin-bottom: 15px;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 1.2em;
            color: #333;
            height: 60px; /* Ajuste da altura */
            box-sizing: border-box;
            transition: all 0.3s ease; /* Transição suave para o efeito de foco */
        }

        .form-control:focus {
            border-color: #5e2a84; /* Cor roxa no foco */
            outline: none;
            box-shadow: 0 0 5px rgba(94, 42, 132, 0.5); /* Efeito de brilho */
            transform: scale(1.05); /* Leve expansão quando o campo estiver em foco */
        }

        /* Estilo do botão */
        button {
            background-color: #5e2a84;
            color: #fff;
            padding: 20px;
            border: none;
            border-radius: 8px;
            font-size: 1.1em;
            cursor: pointer;
            width: 100%;
            transition: background-color 0.3s ease;
            margin-top: 15px;
            height: 60px;
        }

        button:hover {
            background-color: #7a3ba3;
        }

        .login-link {
            font-size: 0.9em;
            color: #333;
            text-align: center;
            margin-top: 10px;
        }

        .login-link a {
            text-decoration: none;
            color: #5e2a84;
        }

        .login-link a:hover {
            text-decoration: underline;
        }

        /* Responsividade */
        @media (max-width: 600px) {
            .input-container {
                flex-direction: column;
            }

            .form-control {
                width: 100%;
                padding: 15px;
                height: 50px;
            }

            button {
                font-size: 1.2em;
                height: 50px;
            }
        }

        /* Animação de fade-in do corpo */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        /* Animação do formulário deslizante */
        @keyframes formSlideIn {
            from {
                transform: translateY(-50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

    </style>
</head>
<body>

    <div class="form-container">
        <!-- Logo -->
        <div class="logo-container">
            <img src="../../nubank.png" alt="Logo">
        </div>

        <h1>Cadastro</h1>
        <form method="POST" action="/cadastro">
            <!-- Nome e Email lado a lado -->
            <div class="input-container">
                <div class="form-group">
                    <label for="nome">Nome Completo</label>
                    <input type="text" id="nome" name="nome" class="form-control" required placeholder="Digite seu nome completo">
                </div>
                <div class="form-group">
                    <label for="email">E-mail</label>
                    <input type="email" id="email" name="email" class="form-control" required placeholder="Digite seu e-mail">
                </div>
            </div>

            <!-- CPF e Telefone lado a lado -->
            <div class="input-container">
                <div class="form-group">
                    <label for="cpf">CPF</label>
                    <input type="text" id="cpf" name="cpf" class="form-control" required placeholder="Digite seu CPF">
                </div>
                <div class="form-group">
                    <label for="telefone">Telefone</label>
                    <input type="text" id="telefone" name="telefone" class="form-control" required placeholder="Digite seu telefone">
                </div>
            </div>

            <!-- Endereço e Data de Nascimento lado a lado -->
            <div class="input-container">
                <div class="form-group">
                    <label for="endereco">Endereço</label>
                    <input type="text" id="endereco" name="endereco" class="form-control" placeholder="Digite seu endereço">
                </div>
                <div class="form-group">
                    <label for="datanasc">Data de Nascimento</label>
                    <input type="date" id="datanasc" name="datanasc" class="form-control" required>
                </div>
            </div>

            <!-- Senha e Confirmar Senha lado a lado -->
            <div class="input-container">
                <div class="form-group">
                    <label for="senha">Senha</label>
                    <input type="password" id="senha" name="senha" class="form-control" required placeholder="Digite sua senha">
                </div>
                <div class="form-group">
                    <label for="confirmar-senha">Confirmar Senha</label>
                    <input type="password" id="confirmar-senha" name="confirmar-senha" class="form-control" required placeholder="Confirme sua senha">
                </div>
            </div>

            <!-- Checkbox Admin -->
            <div class="form-group">
                <input type="checkbox" id="admin" name="admin">
                <label for="admin">Deseja se tornar um Admin?</label>
            </div>

            <button type="submit">Cadastrar</button>
        </form>

        <p class="login-link">
            Já tem uma conta? <a href="/login">Faça login aqui!</a>
        </p>
    </div>

</body>
</html>
